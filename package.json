{
  "name": "reviewable-linker",
  "version": "1.0.0",
  "description": "Slack bot to link Reviewable PRs from a GitHub link",
  "repository": {
    "type": "git",
    "url": "hjdarnel/reviewable-linker"
  },
  "scripts": {
    "start": "npm run test && npm run build",
    "start:dev": "nodemon 'npm run start' | bunyan -o short -l debug",
    "build": "npm run lint && node src/server.js",
    "test": "npm run lint",
    "lint": "eslint src/*.js --cache"
  },
  "author": "Henry Darnell",
  "license": "ISC",
  "dependencies": {
    "@slack/client": "^3.12.0",
    "bunyan": "^1.8.12",
    "lodash": "^4.17.4",
    "eslint": "^4.6.1"
  },
  "devDependencies": {
    "nodemon": "^1.12.0"
  }
}
