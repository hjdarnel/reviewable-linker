{
  "name": "reviewable-linker",
  "version": "1.0.0",
  "description": "Slack bot to link Reviewable PRs from a GitHub link",
  "repository": {
    "type": "git",
    "url": "hjdarnel/reviewable-linker"
  },
  "scripts": {
    "start:dev": "npm install && nodemon --exec 'npm run test && node --inspect src/server.js' | bunyan -o short -l debug",
    "start": "npm install && nodemon src/server.js | bunyan -o short -l debug",
    "test": "npm run lint",
    "lint": "eslint src/**/*.js --cache"
  },
  "author": "Henry Darnell",
  "license": "ISC",
  "dependencies": {
    "@slack/client": "3.12.0",
    "bunyan": "1.8.12",
    "capitalize": "1.0.0",
    "eslint": "4.6.1",
    "github": "13.1.0",
    "lodash": "4.17.4",
    "q": "1.5.1"
  },
  "devDependencies": {
    "nodemon": "1.12.0"
  }
}
